After the actor name e.g. actor1 or thread1 it prints
some weirdf number.

Make it more like the custom_caf-vec application.
Make it so that ShiViz accepts the log files generated.

Fix failing CAF tests

